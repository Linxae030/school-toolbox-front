import React from "react";
import { Target } from "@/apis/target/types";

type IProps = {
  target: Target;
};

const TargetConfigPanel = (props: IProps) => {
  return <div>TargetConfigPanel</div>;
};

export default TargetConfigPanel;
